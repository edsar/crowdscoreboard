<%= form_for(@user_reported_statistic) do |f| %>
  <% if @user_reported_statistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_reported_statistic.errors.count, "error") %> prohibited this user_reported_statistic from being saved:</h2>

      <ul>
      <% @user_reported_statistic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= f.label :user %><br />[<% @users.each do |x| %>  <%= x.id %> (<%=x.twitter_screen_name %>), <%end%>]<br />
      <%= f.text_field :user , :value=>@users.first.id %>
    </div>

    <div class="field">
      <% example = "@c2sb \#g#{@games.first.id}p#{@players.first.id}s#{@statistic_types.first.code}" %>
      <% label = "Tweet : @c2sb #g<game_id>p<player_id>s<stat_code>" %>
      <%=label_tag label %> <br/>
      <%=text_field_tag 'tweet' , value=example %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
    <hr>



 
  <div class="field">
    <%= f.label :statistic_type %><br /> [<% @statistic_types.each do |x| %>  <%= x.id %> (<%=x.code %>), <%end%>]<br />

  </div>
  <div class="field">
    <%= f.label :game %><br /> [<% @games.each do |x| %>  <%= x.id %> (<%=x.name %>), <%end%>]<br />
  </div>
  <div class="field">
    <%= f.label :player %><br />[<% @players.each do |x| %>  <%= x.id %> (<%=x.name %> - <%=x.team.name %>), <%end%>]<br />
  </div>
  <div class="field">
    <%= f.label :user %><br />[<% @users.each do |x| %>  <%= x.id %> (<%=x.twitter_screen_name %>), <%end%>]<br />
  </div>


<% end %>
